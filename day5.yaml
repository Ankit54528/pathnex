#Day 05 — CI/CD and Infrastructure Provisioning

##Jenkinsfile — With Git Integration and Testing

```groovy
pipeline {
    agent any
    stages {
      stage('clone repository') {
        steps {
          git 'https://github.com/Ankit54528/sept2025_Ankit_ankit.git'
          }
      stage('build') {
        steps {
          echo 'building project from Git repository'
          }
          stage('test') {
            steps {
              echo 'running tests'
              }
            }
          stage('deploy') {
            steps {
              echo 'deploying application'
              }
            } 
        }
    }
          
 🔹 GitLab CI/CD — With Docker Build

stages:
  - build
  - push
  - deploy

build:
  stage: build
  script:
    - docker build -t ankit-app .

push:
  stage: push
  script:
    - docker push ankit-app

deploy:
  stage: deploy
  script:
    - kubectl apply -f kubernetes/deployment.yaml
🔹 Ansible — Create User and Set Permissions
- name: Create a new user
  hosts: all
  become: yes
  tasks:
    - name: Create user
      user:
        name: ankit
        state: present
        shell: /bin/bash
    - name: Set permissions for the user
      file:
        path: /home/ankit
        owner: ankit
        group: ankit
        mode: '0755'
        
🔹 Terraform — EC2 with Tags and Volume
resource "aws_instance" "ankitEC2" {
  ami           = "ami-0abcd1234abcd1234"
  instance_type = "t2.medium"
  
  tags = {
    Name = "ankit-Server"
  }

  ebs_block_device {
    device_name = "/dev/sdh"
    volume_size = 50
  }
}